IS IT TAKEN                                          2026-09-04 14:44 UTC

  searched  ########################  7 queries, 2 candidate(s) examined

  idea      a play that finds unused dependencies in a project

VERDICT  ALREADY BUILT
  1 play(s) already do this. Read them before you write a line.

ALREADY DOES THIS
  0.49  example-org/dependency-sweep@0.3.1              18 dl  q=1.00
        Finds dependencies declared in a project that nothing imports, and
        imports that nothing declares. Reads the lockfile and the source,
        never installs.
        shares in the name: dependency

WHAT THIS CANNOT TELL YOU
  Matching is lexical. Two people can describe the same idea in words that
  share nothing, and this will not connect them, so 'nothing close found'
  means no lexical match rather than nothing built. It also only sees
  public plays visible to your session. Read the near matches before
  deciding: the point is not the score, it is that you looked.

  Read-only: it searches the registry and changes nothing.
